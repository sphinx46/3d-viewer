# 3D Viewer

![](https://img.shields.io/badge/Java-17-orange)
![](https://img.shields.io/badge/Maven-3.9.6-blue)

Финальный проект по курсу компьютерной графики Воронежского государственного университета.  
**3D Viewer** — это настольное приложение с открытым исходным кодом, написанное на Java с использованием JavaFX. Оно позволяет загружать, просматривать, редактировать и сохранять 3D-модели и сцены.

---

![Изображение1](images/image1.jpg)

![Изображение2](images/image2.jpg)

![Изображение3](images/image3.jpg)

## Содержание
1. [Обзор проекта](#обзор-проекта)
2. [Основные возможности](#основные-возможности)
3. [Архитектура проекта](#архитектура-проекта)
4. [Установка и сборка](#установка-и-сборка)
5. [Базовый рабочий процесс:](#использование)
6. [Технические детали](#технические-детали)

---

## Обзор проекта

**3D Viewer** — это полнофункциональный 3D-редактор, реализованный на чистой Java без использования сторонних 3D-библиотек (например, OpenGL или LWJGL). Визуализация осуществляется через программный растеризатор, отрисовка — на `Canvas` в JavaFX.

Приложение поддерживает:
- Работу со сложными сценами из нескольких объектов,
- Управление камерами и освещением,
- Применение материалов и текстур,
- Интерактивное редактирование геометрии и трансформаций.

---

## Основные возможности

### 📦 Загрузка и сохранение
- **Поддерживаемые форматы**:
    - Модели: `.obj` (Wavefront)
    - Сцены: собственный бинарный формат `.3dcene`, JSON для импорта/экспорта
    - Текстуры: `.png`, `.jpg`, `.jpeg`, `.bmp`, `.gif`
- **Стандартные примитивы**: куб, сфера, цилиндр, плоскость, капсула, чайник Юты
- **Автосохранение**: автоматическое кэширование путей к последним открытым файлам. Доступ через меню *Файл → Недавние файлы*.

---

### 🎨 Редактирование моделей

#### Трансформация объектов
- **Аффинные преобразования** (в панели **«Трансформация»**):
    - Перемещение по осям X, Y, Z.
    - Вращение вокруг осей X, Y, Z (в градусах).
    - Масштабирование по осям X, Y, Z.
    - Кнопки **«Применить»** и **«Сбросить»** для подтверждения или отмены изменений.

#### Модификация геометрии
- **Удаление элементов** (в панели **«Модификация модели»**):
    - Удаление вершин и полигонов по индексам (можно указать диапазоны: `0,1,2` или `5-10`).
    - Режим «Очистить неиспользуемое» — удаляет вершины, не принадлежащие ни одному полигону.
    - Кнопки **«Выделить»** и **«Снять»** — для управления выделением элементов перед удалением.
- **Статистика модели**:
    - Отображает количество вершин, полигонов, текстурных координат и нормалей в реальном времени.

---

### 🎭 Материалы и отображение

#### Настройка материалов
- **Цвет**: выбор цвета из палитры (White, Red, Blue и др.).
- **Физические свойства**:
    - **Блеск** (`Shininess`) — степень отражения света.
    - **Прозрачность** (`Transparency`) — уровень прозрачности объекта.
    - **Отражение** (`Reflectivity`) — степень зеркального отражения.
- **Текстуры**:
    - Загрузка текстурных изображений с помощью кнопки **«Загрузить»**.
    - Очистка текущей текстуры — кнопка **«Очистить»**.

#### Режимы отображения
- **Текстура** — включает/выключает отрисовку текстур.
- **Освещение** — включает/выключает расчет освещения на основе материала.
- **Полигональная сетка** — отображает каркасную структуру модели.
- **Оси координат** — показывает XYZ-оси для выбранного объекта (включается в панели «Материал»).

> 💡 **Вспомогательные элементы** доступны через меню *Просмотр → Вспомогательные элементы*:
> - **Фоновая сетка** — включает/выключает отображение сетки в сцене.
> - **Координатная ось** — включает/выключает отображение глобальной системы координат.

---

### 🎥 Управление камерами

#### Управление камерами
- **Создание и переключение**:
    - Панель **«Камера»** позволяет создавать и удалять камеры.
    - Выбор активной камеры через выпадающий список.
- **Настройка параметров**:
    - **FOV** (угол обзора) — регулируется слайдером или вручную.
    - **Соотношение сторон** — ширина/высота окна просмотра.
    - **Ближняя/дальняя плоскости отсечения** — определяют диапазон видимости.
    - **Позиция камеры** — координаты X, Y, Z точки, где находится камера.
    - **Цель камеры** — координаты X, Y, Z точки, на которую направлена камера.
    - Кнопки **«Применить»** и **«Сбросить»** для подтверждения изменений.

#### Предустановки и управление
- **Горячие клавиши и меню**:
    - Предустановленные виды: спереди, сверху, слева, справа.
    - Интерактивное управление камерой мышью и клавиатурой (вращение, перемещение, масштабирование).

---

### 🖼️ Интерфейс и UX

#### Структура интерфейса
- **Древовидная панель сцены**:
    - Иерархический список всех объектов сцены.
    - Кнопки **«Добавить»**, **«Удалить»**, **«Дублировать»** для управления объектами.
    - Контекстное меню (правый клик) — переименование, дублирование, удаление, переключение видимости.
- **Раздельные панели свойств**:
    - **Трансформация** — редактирование положения, вращения и масштаба.
    - **Материал** — настройка цвета, текстур и физических свойств.
    - **Модификация** — удаление вершин и полигонов, статистика.
    - **Камера** — управление параметрами и позицией камеры.

#### Дополнительные функции
- **Горячие клавиши** — быстрый доступ к основным действиям (например, `Ctrl+O` — открыть файл, `Ctrl+S` — сохранить и так далее).
- **Темы оформления** — переключение между темной и светлой темами через меню *Тема*.
- **Интерактивные инструменты**:
    - Левая панель с иконками для быстрого доступа к трансформации, модификации и камере.
    - Подсказки (tooltips) для всех кнопок и полей ввода.

> 📘 **Справка** доступна через меню *Справка*:
> - **Документация** — открывает официальную документацию.
> - **Горячие клавиши** — отображает список всех горячих клавиш.
> - **Сообщить об ошибке** — открывает форму для отправки баг-репорта.
> - **О программе** — отображает информацию о версии и авторах.

---

## Архитектура проекта

Проект использует модульную архитектуру с чётким разделением ответственности:

- **`core-engine`** — ядро рендеринга, математика, загрузка моделей, управление сценой
- **`gui-engine`** — JavaFX-интерфейс, контроллеры, обработка событий

---

## Установка и сборка

### Требования
- JDK 17+
- Maven 3.6+
- Любая ОС (Windows, macOS, Linux)

### Сборка
```bash
git clone https://github.com/sphinx46/3d-viewer.git
cd 3d-viewer
mvn clean package
java -jar target/3d-viewer-1.0-SNAPSHOT.jar

```

## Базовый рабочий процесс:

- Создать новую сцену: Файл → Новая сцена
- Добавить модель: загрузить .obj или выбрать примитив
- Редактировать: использовать инструменты трансформации или модификации
- Настроить отображение: материалы, текстуры, освещение
- Сохранить: экспорт модели или всей сцены в JSON / .3dcene
- Горячие клавиши и контекстное меню ускоряют работу. Полный список — в меню Справка → Горячие клавиши.

## Технические детали

- Язык: Java 17
- UI: JavaFX 17+
- Сборка: Maven (multi-module)
- Рендеринг: программный растеризатор (без GPU)
- Форматы: OBJ (Wavefront), JSON (сцены), собственный .3dcene
- Логирование: SLF4J + Logback
- Тестирование: JUnit через maven-surefire-plugin
- Проект полностью самодостаточен: не требует нативных библиотек и работает «из коробки» на любой платформе с JDK 17.

💡 Нашли баг или хотите улучшение?
Пожалуйста, создайте Issue!
